# הוראות קבועות לפרויקט SubHE

## עיקרון על — בלי ניחושים, רק ודאות מוחלטת

**אסור לנחש. פועלים ומדווחים רק על סמך ודאות שנבדקה בפועל.**

- כל טענה עובדתית (מה גורם לבאג, מה שם קובץ, מה ערך בקוד) חייבת להיבדק ישירות
  בקוד/בקבצים/בפלט אמיתי לפני שאומרים אותה. לא "כנראה", לא "אמור להיות", לא הנחה.
- לפני תיקון: לאמת שזו באמת הסיבה. אחרי תיקון: לאמת שהשינוי אכן נכנס ועובד
  (לקרוא את הקובץ שוב, להריץ בדיקה, לוודא JSON/סינטקס תקין).
- אם אין ודאות — לא מנחשים ולא ממציאים; אומרים במפורש "לא בטוח, צריך לבדוק X"
  ובודקים, או שואלים את בעל הרפו. עדיף לומר "לא יודע" מאשר לנחש.
- אין להמציא שמות שדות, כתובות, ערכים או התנהגות של דפדפן/פלטפורמה מהזיכרון —
  לאמת מול מקור אמין או מול הקוד עצמו.

## מדיניות Git — מיזוג ישיר ל‑main

בעל הרפו ביקש במפורש: **תמיד למזג את השינויים ישירות ל‑`main`.**

- לאחר סיום עבודה על ענף פיצ'ר, מזג אותו ל‑`main` ודחוף את `main` ל‑origin.
- העדף מיזוג fast-forward נקי (בלי לשכתב היסטוריה). אם fast-forward לא אפשרי, בצע merge commit רגיל.
- האתר מתפרסם דרך GitHub Pages מענף `main`, ולכן שינוי נחשב "חי" רק אחרי שהוא ב‑`main`.
- אין צורך לפתוח Pull Request או לבקש אישור נוסף כדי למזג ל‑`main` — זו הגדרת ברירת המחדל של הפרויקט.

## הקשר PWA

האתר הוא PWA. נקודות חשובות לשמירה:
- ה‑Service Worker חייב להירשם מקובץ אמיתי (`./sw.js`) — לא דרך `blob:` URL, אחרת Android/Chrome לא יאפשרו התקנת PWA.
- מנגנון עדכון אוטומטי מוגדר ב‑`index.html` (`updateViaCache: 'none'`, `reg.update()` תקופתי, ורענון על `controllerchange`) כדי שהמשתמשים תמיד יקבלו את הגרסה האחרונה. לא להסיר אותו.
